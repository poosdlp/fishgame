JWT_SECRET=supersecret
REFRESH_TOKEN_SECRET=refreshsecret
# Optional override for WebAuthn relying party ID.
# Default when unset: request host (without port), proxy-aware.
RP_ID=

# Optional override for WebAuthn origin.
# Default when unset: request origin built from forwarded proto/host.
EXPECTED_ORIGIN=