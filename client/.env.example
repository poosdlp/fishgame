# Optional. Leave empty to use same-origin requests (recommended behind reverse proxy).
VITE_API_BASE_URL=

# Dev-only convenience override for Vite's /api proxy target.
# Example: http://localhost:3000
VITE_DEV_API_TARGET=
