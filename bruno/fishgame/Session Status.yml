info:
  name: Session Status
  type: http
  seq: 7

http:
  method: GET
  url: http://{{baseurl}}/api/session/{{sessionToken}}/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
