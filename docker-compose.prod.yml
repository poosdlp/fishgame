services:
  client:
    build:
      context: ./client
      target: prod

  server:
    build:
      context: ./server
      target: prod

  traefik:
    build:
      context: ./traefik
    ports:
      - "80:80"
    command:
      - "--entrypoints.web.address=:80"